import { Text } from 'react-native';

import { PlaceholderScreen } from '@/shared/components/PlaceholderScreen';

export function RMHomeScreen() {
  return (
    <PlaceholderScreen title="RM Workspace">
      <Text>Relationship manager module placeholder inside the requested project structure.</Text>
    </PlaceholderScreen>
  );
}
