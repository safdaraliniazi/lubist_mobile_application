import { Text } from 'react-native';

import { PlaceholderScreen } from '@/shared/components/PlaceholderScreen';

export function AdminHomeScreen() {
  return (
    <PlaceholderScreen title="Admin Workspace">
      <Text>Admin module placeholder inside the requested project structure.</Text>
    </PlaceholderScreen>
  );
}
